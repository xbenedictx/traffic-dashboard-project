1. We downloaded a sample csv file named "traffic,csv"
2. Insall the libraries. Run " pip install streamlit pandas plotly" in Terminal.
3. Run the code "streamlit run traffic_dashboard.py" in the Terminal.
